// Private to the service (ARC-2.1): not exported from `index.ts`, so no file
// outside this directory may name it. It is what the rule-2 fixtures reach for.

export function drawFrom(sides: number): number {
    return sides;
}
